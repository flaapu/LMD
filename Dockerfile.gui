FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit==1.33.0 requests==2.31.0

COPY src/ src/

EXPOSE 8501

CMD ["streamlit", "run", "src/gui.py", "--server.port=8501", "--server.address=0.0.0.0"]