name: churn-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pandas
  - scikit-learn
  - pytest
  - pip
  - pip:
      - fastapi
      - uvicorn
      - mlflow
      - pydantic
      - joblib
      - setuptools<70.0.0