Set-Content -Path "environment.yml" -Value @"
name: lmd_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
    - dvc
    - mlflow
    - pandas
    - numpy
    - scikit-learn
    - joblib
    - pyyaml
"@